service: unified
frameworkVersion: '3'

provider:
  name: aws
  runtime: java21

# package:
#   artifact: <+artifact.filePath>

functions:
  currentTime:
    handler: com.serverless.Handler
    # image: <+artifact.image>
    events:
      - httpApi:
          path: /time
          method: get