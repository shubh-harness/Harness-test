secrets:
  secretManagement:
    base:
      secretStore:
        name: project
        kind: ClusterSecretStore
      remoteKeys:
        GLOBAL_TEMPLATE_GIT_TOKEN:
          name: "GLOBAL_TEMPLATE_GIT_TOKEN"
          property: ""
