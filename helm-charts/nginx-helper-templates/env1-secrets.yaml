secrets:
  secretManagement:
    env1:
      secretStore:
        name: env1-vault
        kind: SecretStore
      remoteKeys:
        DATABASE_PASSWORD:
          name: "DB_PASS"
          property: "password"
        API_ENDPOINT:
          name: "API_CONFIG"
          property: "endpoint"
