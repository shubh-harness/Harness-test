secrets:
  secretManagement:
    env2:
      secretStore:
        name: env2-vault
        kind: SecretStore
      remoteKeys:
        API_KEY:
          name: "API_SECRET"
          property: "key"
        REDIS_PASSWORD:
          name: "REDIS_CREDS"
          property: "password"
