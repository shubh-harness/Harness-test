apiVersion: v1
kind: Service
metadata:
  name: {{ include "nginx-with-secrets.fullname" . }}
  labels:
    {{- include "nginx-with-secrets.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nginx-with-secrets.selectorLabels" . | nindent 4 }}
