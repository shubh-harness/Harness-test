apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.service.name | default "nginx-deployment" }}
  namespace: {{ .Values.namespace }}
  labels:
    app: nginx
    shubh: cache
    bla : lab
spec:
  type: {{ .Values.service.type }}
  ports:
  {{- range .Values.service.ports }}
    - port: {{ .port }}
      targetPort: {{ .targetPort }}
      name: {{ .name }}
  {{- end }}
  selector:
    app: nginx
