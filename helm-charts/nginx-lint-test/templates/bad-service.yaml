# apiVersion: v1
# kind: Service
# metadata:
#   name: {{ .Values.releaseName }}-bad-service
#   namespace: {{ .Values.namespace }}
# spec:
#   type: {{ .Values.service.type }}
#   ports:
#     - port: {{ .Values.service.port }}
#       targetPort: {{ .Values.service.targetPort }}
#       protocol: TCP
#       name: http
#   # LINT ISSUE 4: Service with no selector (invalid)
#   # selector field is commented out
#   # selector:
#   #   app: nginx

